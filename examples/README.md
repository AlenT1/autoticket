# Example input files

Drop bug-list markdown files here for testing. Files in this directory are
gitignored by default (see `.gitignore`).

The expected format is documented in `src/file_to_jira/parse/schema.py` (added in Step 3).
The reference real-world example is `Bugs_For_Dev_Review_2026-05-03.md`.
