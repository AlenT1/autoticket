# Environment / secrets — never commit
.env
.env.*
!.env.example
credentials.json
token.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# uv
.venv/

# f2j tool state files
*.state.json
*.state.json.tmp
*.state.json.bak*
state.json
state.json.bak*
state-*.json
state-*.json.bak*
.cache/
.logs/

# jira_task_agent runtime artifacts
# Includes: cache.json, state.json (under data/), gdrive_files/, snapshots/,
# run_plan.json, captured payloads (would_send*.json), cold/cached run debug files.
data/

# Legacy per-operator override at the repo root (shipped defaults live in
# configs/f2j.yaml). New flow: operators edit only `.env`.
/f2j.yaml

# Example input files (drop-in zone; keep dir but ignore content)
examples/*
!examples/README.md

# IDE / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
