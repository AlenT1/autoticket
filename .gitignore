# Secrets — never commit
.env
credentials.json
token.json

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Runtime artifacts (regenerated by the agent each run)
# Includes: cache.json, state.json, gdrive_files/, snapshots/, run_plan.json,
# captured payloads (would_send*.json), cold/cached run debug files.
data/
